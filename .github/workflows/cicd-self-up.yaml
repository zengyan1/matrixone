name: CICD
on:
  push:
    branches:
    - main
    paths:
    - 'cicd/**'

jobs:
  up:
    runs-on: arm64-runner
    steps:
    - uses: actions/checkout@v3
    - uses: ./.github/actions/up
      with:
        dir: cicd
        stack: aws